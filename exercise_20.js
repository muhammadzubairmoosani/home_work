const list = [];
const storeItems = (item) => {
  list.push(item);
};

storeItems("english");
storeItems("urdu");
storeItems("memoni");
storeItems("japanies");
storeItems("chinese");
storeItems("hindi");
storeItems("german");

console.log(list);
