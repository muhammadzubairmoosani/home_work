// below the program return signle value after addition two values
const addition = (a, b) => a + b;

// below the program return signle value after subtration two values
const subtraction = (a, b) => a - b;

console.log(addition(3, 7));
console.log(subtraction(15, 5));
